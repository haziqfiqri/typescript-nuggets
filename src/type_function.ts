function addition (x: number, y: number): number { 
    return x + y
}

const substraction = (x: number, y: number): number => {
    return x + y
}

const logger = (message: string | number): any => { 
    return message
}